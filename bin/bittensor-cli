#!/usr/bin/env python
import bittensor
 
if __name__ == '__main__':
    config = bittensor.Executor.default_config(); 
    print(bittensor.Config.toString(config))
    executor = bittensor.Executor( config )
    executor.run_command()